# Tick Subscription Command

OpenD ticker-flow diagnostic independent from full app. Input: OpenD connection and symbol arguments. Output: console observations. Never trading path. Test/build: `go test ./cmd/tick_sub`; `go build ./cmd/tick_sub`.
