# Commands

Executable composition roots. `etape` runs product; `tick_sub` diagnoses OpenD ticks. Commands own lifecycle/wiring, not domain logic. Children: [etape](etape/README.md), [tick_sub](tick_sub/README.md). Test: `go test ./cmd/...`.
