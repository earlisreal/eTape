# Venue Administration

Coordinates venue configuration, discovery, validation, and lifecycle-visible state. Inputs: settings/credentials; outputs: typed admin results. Never sends orders. Test: `go test ./internal/venueadmin`.
