# Stock Info

Fetches/caches company profile and financial context for focused symbols. Inputs: OpenD requests; outputs: normalized UI snapshots. Context path is non-critical and rate-limited. Test: `go test ./internal/stockinfo`.
