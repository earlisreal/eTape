# Venue Seed

Builds runtime venue instances from validated config and credentials during boot. Outputs adapters plus metadata; errors isolate misconfigured venue where possible. Test: `go test ./internal/venueseed`.
