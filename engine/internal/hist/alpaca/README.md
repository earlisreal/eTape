# Alpaca History

Fetches daily and one-minute market-data history. Only paper credentials may be reused automatically; live execution keys stay isolated. Normalize pagination, ordering, sessions. Test: `go test ./internal/hist/alpaca`.
