# News

Polls OpenD symbol news, normalizes/deduplicates items, publishes current context. Inputs: focused/watchlist symbols; outputs: UI topic. Respect endpoint limits and cancellation. Test: `go test ./internal/news`.
