# Feeds

Normalize market streams into ordered engine events. Current live implementation: [OpenD](opend/README.md). Preserve exchange and receive timestamps. Replay/synthetic sources enter compatible boundaries. Test: `go test ./internal/feed/...`.
