# Mock Engine

Development WebSocket fixture server for fast UI iteration without Go engine/OpenD. Inputs: fixture scenarios; outputs: production-shaped topics/commands. Keep contract aligned with generated types. Run through `./run.sh dev [fixture]`; test via UI suite.
