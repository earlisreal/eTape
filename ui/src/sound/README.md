# Sound

Web Audio order-event cues. Inputs: normalized execution events and preferences; output: local audio. Unlock audio only through user gesture; failures never block execution UI. Test: `npm test -- sound`.
