# Chart Renderer

Chart controllers, history loading, indicators, sessions, drawings, and primitives. Bar store feeds series; visible-range demand may request older history. Preserve chronological merge/dedupe and controller disposal. Test: `npm test -- chart`.
