# Ladder Renderer

Canvas DOM ladder painter. Inputs: order-book/quote state and viewport; output: price-level canvas. Render directly from store snapshots, coalesced per frame. Test: `npm test -- ladder`.
